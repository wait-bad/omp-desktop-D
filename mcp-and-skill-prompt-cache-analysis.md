# MCP 与 Skill 对 Prompt Cache（提示词缓存）的破坏机制与优化实践

> 记录日期：2026-09-10
> 核心主题：大模型工程落地的 Prefix Caching 原理、MCP/Skill 缓存失效陷阱与防护方案

---

## 1. 底层核心：大模型 Prompt Caching 的工作原理

主流大语言模型服务商（Anthropic Claude、OpenAI、Google Gemini 等）采用的均为 **Prefix Caching（严格前缀匹配缓存机制）**。

### 匹配铁律：
- **单向按序比对**：模型在处理输入时，从第一个 Token 开始自左向右、逐字逐字节比对 KV Cache（键值缓存）。
- **前缀强依赖**：只要前面的所有 Token 保持 100% 完全一致，后续内容即可命中缓存。
- **一处变动，全盘失效**：一旦在第 $K$ 个 Token 发生哪怕一个标点、空格或顺序的变动，从第 $K$ 个位置往后的**所有历史缓存全部瞬间作废（Cache Invalidation）**，必须全价重新计算并全额计费。

---

## 2. MCP（Model Context Protocol）为什么是“缓存杀手”？

### ① 异步加载导致的「工具顺序抖动」（非确定性排序）
- **问题**：客户端通常并发连接多个外部 MCP Server（如 GitHub、Obsidian、Chrome DevTools）。网络响应微小的毫秒差异会导致工具注册的先后顺序发生随机变化。
- **后果**：虽然工具内容完全没变，但工具在 Prompt 中的排列顺序发生抖动，导致前缀树直接错位，几万 Token 的系统缓存瞬间蒸发。

### ② 工具定义中的「动态污染」（动态描述/参数）
- **问题**：部分不规范的 MCP 服务会在 Tool Description 或 Enum 参数中塞入实时状态（如当前系统时间戳、当前打开的浏览器标签页列表、随机会话 ID）。
- **后果**：每次发起对话请求时，工具 Schema 本身都是动态变化的，导致缓存永远无法命中（Cache Hit Rate 降为 0%）。

### ③ 运行时动态启用/禁用 MCP 服务
- **问题**：在长对话进行中（如第 10 轮），用户手动在设置中切换开启或关闭某个 MCP 服务。
- **后果**：系统顶部的工具定义集合改变，后续该会话的所有上下文全部失去缓存保护。

---

## 3. Skill（技能）为什么容易破坏缓存？

### ① 运行时动态修改 System Prompt（高位插队）
- **问题**：部分 Agent 框架在识别到特定任务意图时，将长篇技能操作规范（几千 Token）动态注入到最头部的 `System Prompt` 区域。
- **后果**：破坏了整个 Prompt 树的根节点，导致后续累积的长篇对话历史（几十轮）全部无法匹配历史 Cache，代价极其高昂。

### ② 目录扫描的非确定性
- **问题**：本地技能文件夹（如 `~/.codex/skills/`）在读取时依赖操作系统的文件遍历顺序，未做显式排序。
- **后果**：不同平台或文件系统索引变动可能导致 Skill 注入顺序轻微漂移。

---

## 4. 高位骨牌效应（Top-Level Domino Effect）

在现代 Agent 的 Prompt 结构排版中，通常顺序如下：

$$\text{基础 System 规则} \longrightarrow \mathbf{MCP \ 工具声明 (10k\sim 15k\ Tokens)} \longrightarrow \mathbf{Skill \ 技能索引} \longrightarrow \text{多轮对话流水 (User/Assistant)}$$

- **高位敏感性**：MCP 与 Skill 恰好位于整个 Prompt 最核心的**顶层高位**。
- **破坏代价**：一旦高位发生微小变动，后续 30,000 ~ 100,000+ Tokens 的全部对话历史无法享受 90% 的缓存折扣，导致 API 费用暴涨 10 倍，同时带来极高首字延迟（TTFT）。

---

## 5. 业界标准防护与优化方案

1. **确定性字典序排序（Deterministic Sorting）**：
   - 所有从各 MCP 服务汇总的工具列表，在组装进 API 请求前，强制执行按工具名称的 ASCII/Unicode 升序排序。
2. **Schema 绝对纯净静态化**：
   - 工具描述与参数定义严禁包含任何时间戳、动态列表或运行时状态。动态数据一律通过 `tool_result` 工具执行结果在对话流末尾返回。
3. **Skill 按需只读流式加载（On-Demand Tool Reading）**：
   - 绝不在 System Prompt 中动态塞入大段技能正文。
   - 仅在顶部放置固定的极简技能索引列表，当模型需要使用时，由模型自主调用内置只读工具（如 `read("skill://...")`）将内容加载到**对话尾部**，完全不破坏高位前缀缓存。
4. **MCP 服务分组与按需启用**：
   - 对不常用的重型 MCP（如拥有几十个 API 的 DevTools/GitHub）在客户端提供便捷的禁用开关，开局仅挂载必需工具，从源头降低 Context 体积与抖动风险。
